/**
 * Auto-generated TypeScript types from Event Tickets API
 *
 * Regenerate using:
 *   1. Start the backend API: cd src/backend/EventTickets.API && dotnet run
 *   2. Generate spec: curl http://localhost:5000/swagger/v1/swagger.json -o openapi.json
 *   3. Generate types: npx openapi-typescript openapi.json -o src/frontend/src/api/generated/api-schema.ts
 */

// This file will be auto-generated by openapi-typescript
// Placeholder for manual type definitions in the meantime

export interface ApiResponse<T> {
  data: T;
  message?: string;
}

export interface ApiError {
  code: string;
  message: string;
  errors?: Record<string, string[]>;
}

export interface PagedResponse<T> {
  items: T[];
  page: number;
  pageSize: number;
  totalCount: number;
  totalPages: number;
}

export interface EventResponse {
  id: string;
  title: string;
  description?: string;
  venueName: string;
  venueAddress: string;
  venueCity: string;
  startDateTime: string;
  endDateTime: string;
  status: 'Draft' | 'Published' | 'Cancelled' | 'Completed';
  imageUrl?: string;
  totalCapacity: number;
  createdAt: string;
}

export interface TicketTierResponse {
  id: string;
  eventId: string;
  name: string;
  description?: string;
  price: number;
  quantityTotal: number;
  quantitySold: number;
  quantityAvailable: number;
  saleStartDateTime: string;
  saleEndDateTime: string;
}

export interface CartItemResponse {
  ticketTierId: string;
  tierName: string;
  price: number;
  quantity: number;
  subtotal: number;
}

export interface OrderResponse {
  id: string;
  guestName: string;
  guestEmail: string;
  totalAmount: number;
  status: 'Pending' | 'Paid' | 'Cancelled' | 'Refunded';
  paymentCode: string;
  createdAt: string;
  tickets: TicketResponse[];
}

export interface TicketResponse {
  id: string;
  ticketTierId: string;
  tierName: string;
  attendeeName: string;
  attendeeEmail: string;
  qrCode: string;
  status: 'Valid' | 'CheckedIn' | 'Cancelled' | 'Refunded';
  checkedInAt?: string;
}

// Request types
export interface CreateEventRequest {
  title: string;
  description?: string;
  venueName: string;
  venueAddress: string;
  venueCity: string;
  startDateTime: string;
  endDateTime: string;
  imageUrl?: string;
  totalCapacity: number;
}

export interface UpdateEventRequest {
  title: string;
  description?: string;
  venueName: string;
  venueAddress: string;
  venueCity: string;
  startDateTime: string;
  endDateTime: string;
  imageUrl?: string;
  totalCapacity: number;
  status: 'Draft' | 'Published' | 'Cancelled' | 'Completed';
}

export interface EventListResponse {
  events: EventResponse[];
  page: number;
  pageSize: number;
  totalCount: number;
  totalPages: number;
}

// User and Auth types
export type UserRole = 'Admin' | 'Organizer' | 'Attendee';

export interface UserResponse {
  id: string;
  email: string;
  fullName: string;
  role: UserRole;
}

export interface AuthResponse {
  token: string;
  refreshToken: string;
  expiresAt: string;
  user: UserResponse;
}

export interface CreateTicketTierRequest {
  eventId: string;
  name: string;
  description?: string;
  price: number;
  quantityTotal: number;
  saleStartDateTime: string;
  saleEndDateTime: string;
}

export interface AddToCartRequest {
  ticketTierId: string;
  quantity: number;
}

export interface CreateOrderRequest {
  guestName: string;
  guestEmail: string;
  items: CreateOrderItemRequest[];
}

export interface CreateOrderItemRequest {
  ticketTierId: string;
  quantity: number;
}
